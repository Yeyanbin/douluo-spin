# v0.2 实施过程记录

> 开始时间：2026-07-16
> 状态：进行中

---

## 阶段 A — Handler 链路补全

### 计划

- A-1: `applyCommon()` 新增容貌变化解析（「容貌+1」→ 容貌 B→A 升级）
- A-2: `switch` 新增 `case 'beastEncounter':`，解析修为增减写入 `context.beast.cultivation`

### 实施

**A-1 applyCommon 容貌变化**

| 项目 | 内容 |
|------|------|
| 位置 | `src/domain/machine.ts` → `applyCommon()` |
| 逻辑 | 正则匹配 `容貌\s*([+-])\s*(\d+)`，在 GRADE 数组 F→E→D→C→B→A→S→EX 中移动 |
| EX 封顶 | 文本含 `ex级?无法提升` 则不操作 |
| 风险 | 容貌初始值如果在数组中找不到（如"未确定"），跳过 |



**A-2 beastEncounter handler**

| 项目 | 内容 |
|------|------|
| 位置 | `src/domain/machine.ts` → `applyResult()` switch |
| 逻辑 | 匹配 `修为+N年`、`修为掉落N年`、`修为-N年`，更新 `cultivation`，下限 10 年 |
| 靠前 | 插在 `beastGrowth` 之前 |



### 结果

- 构建：
- 测试：
- commit：

---

## 阶段 B — 战斗数值化

### 计划

- B-1: 嘉陵关 12 个子池的选项嵌入等级/称号变化
- B-2: 神战 8 个池的选项嵌入等级/称号变化
- B-3: 大赛（预选赛/单人赛/决赛）选项嵌入等级/称号变化

### 实施



### 结果

- 构建：
- 测试：
- commit：

---

## 阶段 C — 势力与学院数值化

### 计划

- C-1: 8 个势力 120 个选项嵌入等级/称号变化（`canonAdditions.ts`）
- C-2: 学院池（入学/在校/去向）选项嵌入等级/称号变化（`wheels.json`）

### 实施



### 结果

- 构建：
- 测试：
- commit：

---

## 阶段 D — 武魂分级 + 战力 UI

### 计划

- D-1: `CharacterPanel.vue` 新增武魂阶位显示
- D-2: `CharacterPanel.vue` 新增战力值显示

### 实施



### 结果

- 构建：
- 测试：
- commit：

---

## 阶段 E — 测试更新

### 计划

- E-1: 更新 `catalog.test.ts` 数据期望
- E-2: 更新 `machine.test.ts` 权重期望

### 实施



### 结果

- 构建：
- 测试：
- commit：

---

## 阶段 F — 100 轮回归验证

### 计划

- F-1: 运行 `npm run simulate:long`
- F-2: 检查 `audit-report.md`
- F-3: 如有问题，返回修复

### 实施



### 结果

- 归档目录：
- 通过率：
- 问题：
